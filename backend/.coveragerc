[run]
source = app
# SQLAlchemy's async engine runs code inside greenlets. Without this,
# coverage silently misses every line that executes after an awaited query.
concurrency = greenlet, thread

[report]
show_missing = true
skip_empty = true
